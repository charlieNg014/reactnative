import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class ShopDetails extends Component {
    render() {
        return (
            <View>
                <Text> Shops Details </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
